function User(firstName, lastName, email, premiumStatus) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.email = email;
    this.premiumStatus = premiumStatus;

    // Returns a list of all unlocked levels by the user
    this.unlockedLevels = function() {

    }

    // Returns the last unlocked level
    this.currentLevel = function() {

    }

    // Calls the API to flag the given level as completed
    this.completeLevel = function(levelIndex) {

    }
}