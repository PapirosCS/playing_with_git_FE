class User {
    constructor (firstName, lastName, email, premiumStatus) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.email = email;
        this.premiumStatus = premiumStatus;
    }


    // Returns a list of all unlocked levels by the user
    unlockedLevels() {

    }

    // Returns the last unlocked level
    currentLevel() {

    }

    // Calls the API to flag the given level as completed
    completeLevel(levelIndex) {

    }
}